## 1. Watched-item reminder policy

- [x] 1.1 Add `Tests/AgentCoreTests/WatchedItemReminderPolicyTests.swift` with `testItemAtThresholdMakesEnabledFolderEligible` and `testRecentProcessedAndDisabledItemsStayIneligible`; assert the exact threshold is eligible while younger, processed, and disabled observations are not, then run `build-gate -- swift test --filter WatchedItemReminderPolicyTests` and observe failure because the public reminder policy is absent
- [x] 1.2 Add the opaque watched-item observations, positive threshold validation, injected-clock folder policy, and generic private reminder value in `AgentCore`; run `build-gate -- swift test --filter WatchedItemReminderPolicyTests` and verify the threshold suite passes
- [x] 1.3 Add `testContinuingConditionIsSuppressedUntilCleared`, `testSnoozeAndDeniedPermissionSuppressDelivery`, and `testReminderStateRoundTripsWithoutFolderCoordinates` to the reminder test suite; assert one continuing condition cannot notify twice, clearing rearms it, denied/snoozed evaluations stay quiet, and persisted state contains no path/name, then run the filtered suite and observe the new assertions fail because suppression coordination and storage are absent
- [x] 1.4 Implement the atomic reminder-state document and coordinator over the shared notification authorization/delivery boundary, recording delivery only after acceptance and resetting after the eligible condition clears; run `build-gate -- swift test --filter WatchedItemReminderPolicyTests` and `build-gate -- swift test --filter ImportNotificationTests` and verify both suites pass

## 2. Operational diagnostics

- [x] 2.1 Add `Tests/AgentCoreTests/OperationalDiagnosticsTests.swift` with `testAssemblerPreservesMixedPermissionDiskJournalAndQueueState`, `testCorruptJournalMakesQueueUnavailable`, and `testUpdateCheckIsDeferred`; assert exact folder-state counts, notification status, available bytes, journal attention state, trustworthy queue depth, and the inert item-10 update state, then run `build-gate -- swift test --filter OperationalDiagnosticsTests` and observe failure because the public diagnostics assembler is absent
- [x] 2.2 Implement typed folder/notification/disk/journal/queue/update observations, the immutable diagnostics snapshot, and narrow disk/journal probes without raw errors or paths; run `build-gate -- swift test --filter OperationalDiagnosticsTests` and verify the diagnostics assembly suite passes
- [x] 2.3 Add `Tests/RatatoskrExportAgentTests/DiagnosticsPresentationTests.swift` with `testStatusMenuOpensDiagnostics` and `testDiagnosticsRowsExposeEveryRequiredCategoryWithoutCoordinates`; assert the menu action is wired and the view model exposes permission, disk, journal, queue, and deferred-update rows with no path/URL fields, then run `build-gate -- swift test --filter DiagnosticsPresentationTests` and observe failure because the diagnostics presentation is absent
- [x] 2.4 Add the diagnostics window/view model/SwiftUI panel and status-menu routing over injected production probes, keeping unavailable sources explicit; run `build-gate -- swift test --filter DiagnosticsPresentationTests` and verify the presentation suite passes

## 3. Redacted support report

- [x] 3.1 Add `Tests/AgentCoreTests/SupportReportRedactionTests.swift` with `testDefaultReportExcludesSensitiveCanaries` and `testFreeFormFailureMapsToBoundedClassification`; seed filenames, paths, content, credential-bearing URLs, full digests, tokens, and backend text outside the closed input, assert none occurs in encoded bytes while counters/enums and a fixed digest prefix do, then run `build-gate -- swift test --filter SupportReportRedactionTests` and observe failure because the report builder is absent
- [x] 3.2 Implement the closed Codable report schema, sanitized item summary, fixed digest-prefix validation, bounded classifications, and deterministic JSON builder with no arbitrary metadata or raw-error input; run `build-gate -- swift test --filter SupportReportRedactionTests` and verify the default redaction suite passes
- [x] 3.3 Add `testExplicitSelectionIncludesOnlyOneReviewedField`, `testPathsAndCredentialsAreRefusedEvenWhenSelected`, and `testExporterWritesExactlyPreviewedBytesLocally` to the support report suite; assert selection is per item and field, prohibited values fail closed, and a fixture exporter receives byte-for-byte preview data without a network seam, then run the filtered suite and observe the new assertions fail because explicit selection and export are absent
- [x] 3.4 Implement bounded per-item field selection, validation, preview data, and a local atomic report writer, then wire preview/review/save actions into diagnostics; run `build-gate -- swift test --filter SupportReportRedactionTests` and `build-gate -- swift test --filter DiagnosticsPresentationTests` and verify both suites pass

## 4. Documentation and full validation

- [x] 4.1 Update `README.md`, `docs/ARCHITECTURE.md`, `docs/TESTING.md`, and `docs/IMPLEMENTATION_PLAN.md` with delivered reminder/diagnostics/report behavior, privacy/permission/disk impact, and the explicitly deferred item-10 update decision; no failing test applies because this is documentation, so verify with `rg -n "support report|Diagnostics|update.*defer|reminder"` over those files
- [x] 4.2 Review the final diff for sensitive-field escape hatches and scope creep, then run the repository commands from `DEVELOPMENT.md` through `build-gate` where compiler-backed, plus `openspec validate --all --strict` and `openspec validate --archived`; verify every command exits successfully before marking the change ready to archive
