## 1. Bundle, sandbox, and release validation

- [x] 1.1 Add failing shell tests `test_package_requires_versions` and `test_package_builds_expected_bundle` in `Tests/Distribution/PackageAppTests.sh`; run them and confirm they fail because the packaging entry point does not exist, while asserting missing versions are rejected and valid versions produce the required bundle metadata/layout.
- [x] 1.2 Add `Distribution/Info.plist.template` and `scripts/distribution/package-app.sh`, then run `Tests/Distribution/PackageAppTests.sh` and verify both packaging tests pass against an explicit prebuilt release executable.
- [x] 1.3 Add failing shell tests `test_entitlements_match_exact_allowlist` and `test_unexpected_entitlement_is_rejected` in `Tests/Distribution/EntitlementsPolicyTests.sh`; run them and confirm they fail because the policy/verifier does not exist, with assertions for exactly sandbox, user-selected read/write, and outbound network client keys.
- [x] 1.4 Add `Distribution/RatatoskrExportAgent.entitlements` plus entitlement-policy validation in `scripts/distribution/verify-app.sh`, then run `Tests/Distribution/EntitlementsPolicyTests.sh` and verify the committed allowlist passes and an injected broad filesystem entitlement fails with its key named.
- [x] 1.5 Add failing shell tests `test_unsigned_bundle_is_not_release_ready` and `test_ad_hoc_bundle_is_not_release_ready` in `Tests/Distribution/SignaturePolicyTests.sh`; run them and confirm they fail because release-signature validation is absent, while asserting neither fixture can be labeled Developer ID/hardened/notarized evidence.
- [x] 1.6 Implement staged bundle validation in `scripts/distribution/verify-app.sh` for structure, Developer ID Application authority, secure timestamp, hardened runtime, exact signed entitlements, stapled ticket, and Gatekeeper assessment; run `Tests/Distribution/SignaturePolicyTests.sh` and verify unsigned/ad hoc bundles are rejected in release mode.
- [x] 1.7 Add failing shell test `test_ad_hoc_sandbox_bundle_smoke_launches_without_release_claim` in `Tests/Distribution/SandboxSmokeTests.sh`; run it and confirm it fails before the distribution test harness can assemble and ad hoc sign a sandbox bundle.
- [x] 1.8 Add `Tests/Distribution/run.sh` and the sandbox smoke harness, then run it and verify the packaged app reaches the existing `--smoke` startup under the committed sandbox entitlements while the same artifact remains rejected by release validation.

## 2. Manual update behavior and diagnostics

- [x] 2.1 Add failing tests `testManualUpdateDiagnosticsDecodesExactCurrentVersion`, `testManualUpdateDiagnosticsDecodesUnavailableVersionWithoutGuess`, and the revised update-diagnostics assertion in `Tests/AgentCoreTests/ApplicationUpdatePolicyTests.swift` and `OperationalDiagnosticsTests.swift`; run the focused tests and confirm they compile but fail because the existing Codable state and assembler support only the deferred distribution decision. The fixed URL is proven through the explicit opener in task 2.3, avoiding a test reference to a not-yet-existing production type.
- [x] 2.2 Implement the dependency-free manual update policy and version-aware `UpdateCheckDiagnostics` in `AgentCore`, update snapshot assembly and diagnostics rows, then run the focused core/diagnostics tests and verify truthful version/unavailable states and zero update network seam.
- [x] 2.3 Refactor `AgentMenuCoordinator` to accept injected URL-opening and generic-failure presentation closures while adding no update action; run existing `StatusItemMenuTests` and `DiagnosticsPresentationTests` and verify behavior remains green. This behavior-neutral seam follows existing green tests and lets the next red test compile without a placeholder product path.
- [x] 2.4 Add failing tests `testMenuIncludesCheckForUpdatesAction`, `testCheckForUpdatesOpensReleasePageOnce`, and `testCheckForUpdatesFailureShowsGenericAlert` in `Tests/RatatoskrExportAgentTests/ManualUpdateMenuTests.swift`; run them and confirm they compile but fail because the menu has no update action.
- [x] 2.5 Implement the `Check for Updates…` menu action with the injected opener/presenter and production `NSWorkspace` adapter, then run `ManualUpdateMenuTests` and verify one explicit open of only the fixed release URL plus generic failure handling.

## 3. Owner-authorized signing and notarization CI

- [x] 3.1 Add failing shell tests `test_missing_secrets_lists_every_name_without_values` and `test_complete_secret_set_passes` in `Tests/Distribution/OwnerSecretValidationTests.sh`; run them and confirm they fail because no CI-secret validator exists.
- [x] 3.2 Implement `scripts/distribution/validate-owner-secrets.sh` for the five named owner secrets, then run `OwnerSecretValidationTests.sh` and verify missing names are complete, supplied values never appear, and a complete injected set passes.
- [x] 3.3 Add failing shell test `test_distribution_workflow_is_fail_closed` in `Tests/Distribution/WorkflowContractTests.sh`; run it and confirm it fails because `.github/workflows/distribution.yml` and signing/notarization scripts are absent, while asserting manual trigger, ephemeral Keychain cleanup, Developer ID/hardened signing, `notarytool --wait`, stapling, post-validation, and artifact publication ordering.
- [x] 3.4 Implement `.github/workflows/distribution.yml` and `scripts/distribution/sign-and-notarize.sh`, using only temporary owner-secret material and publishing the final ZIP only after every release validation; run `WorkflowContractTests.sh` and a local no-credential dry run to verify exact fail-closed behavior. Configuration cannot start from a live failing notarization test because owner-held credentials are intentionally unavailable to local development.
- [x] 3.5 Record credential-backed evidence in `docs/DISTRIBUTION.md` if an owner-authorized CI run is available; otherwise record the exact five missing GitHub Actions secrets, required Apple account assets, and the precise workflow dispatch needed to unblock notarized evidence. Documentation cannot start from a failing product test because it records an external authority blocker rather than runtime behavior.

## 4. ADR, security rationale, and workspace integration

- [x] 4.1 Add accepted `docs/adr/0007-sandbox-signing-notarization-and-manual-updates.md` with the repository-required context, options, decision, consequences, security/privacy, lifecycle/distribution, schema impact, validation, and follow-up, and update `docs/adr/README.md`; verify it records manual download over Sparkle and the exact entitlement rationale. Documentation cannot start from a failing test because it records a hard-to-reverse architectural decision.
- [x] 4.2 Add `docs/DISTRIBUTION.md` and `docs/WORKSPACE_INTEGRATION.md`, and update `README.md`, `DEVELOPMENT.md`, `docs/ARCHITECTURE.md`, `docs/TESTING.md`, `docs/THREAT_MODEL.md`, and `docs/IMPLEMENTATION_PLAN.md` to match delivered behavior, commands, permissions, blockers, compatibility, rollout/rollback, old-agent behavior, privacy impact, and the absent workspace pin/harness. Documentation cannot start from a failing product test; verify every status statement distinguishes local structure, normal CI, owner-authorized notarization, clean-machine acceptance, and workspace integration evidence.
- [x] 4.3 Add the unsigned distribution tests to `.github/workflows/ci.yml` and keep `DEVELOPMENT.md`'s command list byte-for-byte aligned; verify the existing drift check succeeds. CI configuration cannot start from a separate failing product test because it wires already-failing tests from tasks 1-3 into the required gate.

## 5. Validation and delivery readiness

- [x] 5.1 Run the focused Swift and distribution tests named above, `plutil` on bundle metadata/entitlements, and unsigned/ad hoc negative release checks; verify each behavior task's test was observed red before implementation and is now green.
- [x] 5.2 Run the full machine-gated project command list from `DEVELOPMENT.md`, `openspec validate --all --strict`, and `openspec validate --archived`; inspect the final diff for secrets, broad entitlements, unrequested dependencies, stale status claims, and unrelated edits, and record whether notarization is proven or blocked on the exact owner credentials.
