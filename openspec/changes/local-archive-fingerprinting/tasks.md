# Tasks: local-archive-fingerprinting

Each behaviour below lands as a pair: the first task adds a failing test (file, test name, and assertion named), the second makes it pass. Where a new API would otherwise fail to compile, the failing-test task declares the minimal stub so the observed failure is the behavioural assertion, not a build error. Tooling, documentation, and pure-glue tasks carry their one-line justification for not starting from a failing test.

## 1. Streaming fingerprinting (AgentCore)

- [x] 1.1 Add failing tests in `Tests/AgentCoreTests/StreamingHasherTests.swift`: `testEmptyFileYieldsKnownEmptyDigest` (zero-byte fixture reports `e3b0c442...b855` with size 0), `testShortInputMatchesPublishedVector` (`abc` reports `ba7816bf...15ad` with size 3), `testChunkedFixtureWritesHashCorrectly` (a fixture larger than one hash chunk built from many small appends hashes to the digest CryptoKit one-shot computes over the same logical bytes, with size matching disk), and `testShrinkingSourceFailsWithoutDigest` (file truncated below observed size mid-hash throws instead of reporting a value). Declare `StreamingHasher` as a minimal stub whose hash always throws so failures are behavioural. Confirm they fail.
- [x] 1.2 Implement `StreamingHasher` over CryptoKit's incremental SHA256 with bounded chunk reads, expected-size tracking, and lowercase hex output; verify it never mutates the source. Verify: `swift test` green for 1.1.
- [x] 1.3 Add failing test `testHashingLeavesSourceUntouched` in the same file: content bytes, size, and modification time of a fixture are identical before and after hashing. Note: passed on its first run against the read-only implementation from 1.2 - no red phase was observable; retained as the immutability pin. Verify: `swift test` green for 1.3 (`Executed 5 tests`, filter exit 0).

## 2. Shallow classification (AgentCore)

- [ ] 2.1 Add failing tests in `Tests/AgentCoreTests/ArchiveClassifierTests.swift` with synthetic fixtures built in-test: `testZipMagicBytesAreRecognized` and `testJsonShapeIsRecognizedAndPlainTextIsUnknown` (container sniffing), `testChatGPTMarkersLabelChatGPTStrongly` (top-level `conversations.json` + `user.json` → chatgpt strong with marker evidence), `testClaudeMarkersLabelClaudeStrongly` (`conversations.json` + `users.json` → claude strong), `testInstagramActivityFolderLabelsInstagram`, `testThreadsMarkersLabelThreads`, `testOverlappingWeakEvidenceReportsAmbiguous` (partials across two provider rows → ambiguous, not a guess), `testMarkerlessZipIsUnidentifiedButStillZip`, and `testUnrecognizableBinaryClassifiesCleanly` (unknown container + unidentified, no throw). Declare `ArchiveClassifier` plus its result types as stubs returning unknown/unidentified so failures are behavioural. Confirm they fail.
- [ ] 2.2 Implement the classifier: magic-byte container sniffing, bounded central-directory name reading for zips (EOCD scan within the last 64 KiB, entry-name cap), bounded top-level JSON probing (first 64 KiB prefix; root keys or first-array-element keys), and the data-driven provider marker table with strong/probable/ambiguous/unidentified outcomes and recorded evidence. Verify: `swift test` green for 2.1.
- [ ] 2.3 Add failing test `testLargeCentralDirectoryStaysWithinBounds` in the same file: a zip whose central directory exceeds the bounded window still classifies without reading beyond the window and terminates promptly with an evidence-backed outcome. Confirm it fails against unbounded assumptions if observable, else note why retained. Verify: `swift test` green for 2.3.

## 3. Immutable archive store (AgentCore)

- [ ] 3.1 Add failing tests in `Tests/AgentCoreTests/LocalArchiveStoreTests.swift` using real temp directories and synthetic candidates: `testArchivingPublishesVerifiedDigestAddressedCopy` (store file at `<provider>/<year>/<month>/<digest>.<ext>` hashes to the recorded digest, reported size matches, original remains untouched), and `testDuplicateArchivalReusesStoredEntryWithoutRewrite` (second run returns same path; modification time/inode unchanged; no temp files left). Declare `LocalArchiveStore`, its result type, and error enum as stubs so failures are behavioural. Confirm they fail.
- [ ] 3.2 Implement single-pass copy+hash publication: stream source into a `.tmp-<digest>-*` file inside the destination directory while hashing, verify copied digest, then rename to the final name; on existing final path compare digests - match returns existing write-once, mismatch raises integrity error. Verify: `swift test` green for 3.1.
- [ ] 3.3 Add failing tests in the same file: `testInterruptionMidCopyPublishesNothing` (injected hook throws after partial copy → no entry at the final path, store layout holds no visible partial archive) and `testRetryAfterInterruptionSucceeds` (rerun without interruption publishes verified bytes). Confirm they fail.
- [ ] 3.4 Implement the interruption seam and stale-temporary sweep: publish hooks fire per chunk flush and before rename; stale `.tmp-*` files from prior runs are swept before copying so interrupted runs recover idempotently. Verify: `swift test` green for 3.3.
- [ ] 3.5 Add failing test `testMismatchedContentAtOccupiedPathFailsExplicitly` in the same file: pre-place different bytes at the target path → archival fails with an explicit integrity error and the occupying file is byte-for-byte unchanged. Confirm it fails.
- [ ] 3.6 Fold the mismatch branch into publish: occupied-path handling verifies existing bytes and refuses displacement. Verify: `swift test` green for 3.5.

## 4. Disk budget and configuration wiring

- [ ] 4.1 Add failing tests in `Tests/AgentCoreTests/LocalArchiveStoreTests.swift`: `testOverBudgetRefusalHappensBeforeAnyCopying` (pre-filled store plus incoming size above budget → explicit over-budget error naming the limit, no new or temp files anywhere in the store) and `testWithinBudgetArchivalProceeds` (projected usage under budget succeeds and store total grows by exactly the incoming size). Also add failing tests in `Tests/AgentCoreTests/ConfigurationLoadingTests.swift`: `testMissingStoreBudgetDefaultsToDocumentedDefault` and `testNonPositiveStoreBudgetRejectedNamingField`. Declare the budget parameter/error cases as stubs so failures are behavioural. Confirm they fail.
- [ ] 4.2 Implement budget enforcement (walk store summing regular-file sizes before any mutation; refuse when projected usage exceeds the limit) and extend schema-version-1 configuration in place with optional `maxArchiveStoreBytes` defaulting when absent and validated positive. Verify: `swift test` green for 4.1.

## 5. Documentation and full gate

- [ ] 5.1 Update README status block and docs references: fingerprinting, shallow classification, immutable local archive, atomic publication, and disk budget exist; journal, upload, Keychain, notifications remain future work. Documentation change, no failing test applies. Verify: status block matches this change's scope with no overclaim.
- [ ] 5.2 Run the complete gate: `swift build`, `swift test`, `swift build -c release`, `.build/release/RatatoskrExportAgent --smoke`, `swiftlint`, `openspec validate --all --strict`, and `openspec validate --archived`, all green. This is the cross-cutting verification task spanning all groups above. Record observed counts in this task.

## 6. Ship

- [ ] 6.1 Commit the work on `feat/local-archive-fingerprinting` as conventional commits grouped by concern (streaming hash, classifier, archive store, budget/configuration, docs/gate). Verify: `git log` shows the grouped history and the worktree diff is fully committed.
- [ ] 6.2 Merge into `main`, push `origin main`, then remove the worktree and delete the feature branch. Verify: remote `main` contains the merge commit; `git worktree list` and the branch list no longer contain this task's entries.
